import styled from 'styled-components';

import skullLogo from './../../imgs/skull.png';

function Logo() {
  return <LogoImg src={skullLogo} />;
}

export const LogoImg = styled.img``;

export default Logo;
